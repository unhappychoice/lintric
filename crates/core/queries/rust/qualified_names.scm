; The name after :: is reached through its path, not through a lexical import.
; Nested paths also capture intermediate names without relying on whitespace.
(scoped_identifier name: (_) @qualified)
(scoped_type_identifier name: (_) @qualified)
